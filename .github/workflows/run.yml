name: Download Report

on:
  schedule:
    - cron: "0 * * * *"   # every hour
  workflow_dispatch:      # manual trigger

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium   # ✅ ensures Chromium binary + deps are installed

      - name: Run download script
        env:
          SITE_USER: ${{ secrets.SITE_USER }}
          SITE_PASS: ${{ secrets.SITE_PASS }}
        run: python scripts/download_report.py

      - name: Commit & Push report
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/report.csv
          git commit -m "Auto report update" || echo "No changes to commit"
          git push
